# %APPDATA%\yazi\config\keymap.toml
# Lean keymap on top of yazi defaults. Adds zoxide jump on `z` and a quick
# parent-shell-cd on `Q`.

[[mgr.prepend_keymap]]
on   = "z"
run  = "plugin zoxide"
desc = "Jump via zoxide"

[[mgr.prepend_keymap]]
on   = "Z"
run  = "plugin fzf"
desc = "Jump via fzf"
